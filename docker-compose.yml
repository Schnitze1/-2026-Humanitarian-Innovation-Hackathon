version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: aiga_postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - aiga_pgdata:/var/lib/postgresql/data

volumes:
  aiga_pgdata:
